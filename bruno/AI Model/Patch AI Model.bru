meta {
  name: Patch AI Model
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/aiModel/{{aiModelId}}
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:json {
  {
    "state": "inactive"
  }
}
