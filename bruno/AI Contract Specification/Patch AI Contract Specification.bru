meta {
  name: Patch AI Contract Specification
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/aiContractSpecification/{{aiContractSpecId}}
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
}

body:json {
  {
    "state": "inactive"
  }
}
